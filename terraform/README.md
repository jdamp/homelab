# Terraform

This folder contains Terraform configurations for this homelab.

## Layout

- `proxmox/`: infrastructure provisioning on Proxmox (VMs, inventory output, and related configuration).
  - See `proxmox/README.md` for usage.
