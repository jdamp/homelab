apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: paseo

resources:
  - sealed-secrets.yaml
  - serviceaccount.yaml
  - pvc-home.yaml
  - pvc-workspace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
