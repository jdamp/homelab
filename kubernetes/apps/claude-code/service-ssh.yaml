apiVersion: v1
kind: Service
metadata:
  name: claude-code-ssh
spec:
  type: NodePort
  selector:
    app: claude-code
  ports:
    - name: ssh
      port: 22
      targetPort: 22
      nodePort: 30022
      protocol: TCP
