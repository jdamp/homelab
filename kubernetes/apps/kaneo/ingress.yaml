apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: kaneo-ingress
  namespace: kaneo
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`todo.mauzlab.de`)
      services:
        - name: kaneo-kaneo
          port: 5173
  tls: {}
