apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: kaneo

helmCharts:
  - name: kaneo
    repo: oci://ghcr.io/usekaneo/charts
    version: 2.24.0
    releaseName: kaneo
    valuesFile: kaneo-values.yaml

resources:
  - sealed-secrets.yaml
  - oidc-sealed-secrets.yaml
  - ingress.yaml
