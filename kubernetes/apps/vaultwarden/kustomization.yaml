apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: vaultwarden

resources:
  - oidc-sealed-secrets.yaml
  - deployment.yaml
  - service.yaml
  - pvc.yaml
  - ingress.yaml
  - postgres/sealed-secrets.yaml
