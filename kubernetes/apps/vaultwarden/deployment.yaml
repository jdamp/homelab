apiVersion: apps/v1
kind: Deployment
metadata:
  name: vaultwarden-deployment
  labels:
    app: vaultwarden
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vaultwarden
  template:
    metadata:
      labels:
        app: vaultwarden
    spec:
      restartPolicy: Always
      containers:
      - name: vaultwarden
        image: vaultwarden/server:1.35.1-alpine
        imagePullPolicy: IfNotPresent
        env:
        - name: DOMAIN
          value: "https://passwort.mauzlab.de"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: postgres-cluster-app
              key: uri
        ports:
        - containerPort: 80
          name: http
        volumeMounts:
        - name: vaultwarden-storage
          mountPath: /data/
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /alive
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /alive
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 5
      volumes:
      - name: vaultwarden-storage
        persistentVolumeClaim:
          claimName: vaultwarden-data-pvc