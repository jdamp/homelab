apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hortusfox-app-pvc
  labels:
    app: hortusfox
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
