apiVersion: v1
kind: Service
metadata:
  name: mealie-service
  labels:
    app: mealie
spec:
  type: ClusterIP
  selector:
    app: mealie
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 9000
